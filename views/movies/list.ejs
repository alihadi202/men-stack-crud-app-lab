<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>movies Index</title>
  </head>

<body>
    <%- include('../partials/nav') %>
    <h1>All movies</h1>
    <ul>
        <% movies.forEach(movie => { %>
            <li><a href="/movies/<%= movie._id %>"><%= movie.name %></a></li>
          <% }); %>
    </ul>
  </body>
  
</html>